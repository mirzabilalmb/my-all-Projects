// main.js - small UI helpers
function confirmDelete() {
  return confirm('Are you sure you want to delete this book? This cannot be undone.');
}

// Optional: add keyboard shortcuts, etc. (left intentionally small)
